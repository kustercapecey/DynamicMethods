using UnityEngine;

public class CountdownTimer : MonoBehaviour
{
    public float timer = 10f;

    void Update()
    {
        timer -= Time.deltaTime;
        if (timer <= 0)
        {
            Debug.Log("Time's up!");
        }
    }
}
