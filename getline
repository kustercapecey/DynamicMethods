#include <iostream>
using namespace std;

int main() {
    string str;
    cout << "请输入一个字符串: ";
    getline(cin, str);

    int length = 0;
    for (char c : str) {
        length++;
    }

    cout << "字符串长度为: " << length << endl;
    return 0;
}
